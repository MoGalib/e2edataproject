version: 2

models:
  - name: address
    description: "Address model"
    columns:
      - name: Address_ID
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
